import 'package:flutter/material.dart';
import 'package:trust_chain/profile.state.dart';

class Profile extends StatefulWidget {
  Profile({Key key}) : super(key: key);

  @override
  ProfileState createState() => ProfileState();
}

